##Develop info

in order to build the JS file following commands has to be runned:

```bash
cd resources/private/js
npm run install
npm run watch
```

Now run amigo 

```bash
cd app
go run main.go
```
